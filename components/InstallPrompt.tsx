import React from "react";

/**
 * InstallPrompt (React Native)
 * - Returns null since PWA installation prompts are not needed in a native mobile app.
 * - Created to maintain file path matching and avoid import compiler errors in migrated files.
 */
export default function InstallPrompt() {
  return null;
}
